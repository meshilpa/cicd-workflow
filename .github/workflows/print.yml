name: Print Message

on:
    push:
          branches:
              - master

jobs:
    say-hello:
        runs-on: ubuntu-latest

        steps:
            - name: Print a Message
              run: echo "Hello! A push detected in repo"